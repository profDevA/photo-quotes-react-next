import React from 'react'

function Error () {
    return(
        <div style={{height:'100vh', display:'flex', alignItems:'center', justifyContent:'center'}}>
            <p style={{fontSize:30}}>404 error</p>
        </div>
    )
}

export default Error